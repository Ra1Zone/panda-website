// Legacy file — admin layout is now at src/app/admin/(panel)/layout.tsx
// This stub exists only to prevent import errors from any remaining references.
"use client";
export const AdminLayout = ({ children }: { children?: React.ReactNode }) => <>{children}</>;
export default AdminLayout;
